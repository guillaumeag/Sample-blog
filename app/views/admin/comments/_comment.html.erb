<li>
  <span>
    <strong><%= comment.author %></strong>
    <span><%= comment.created_at.to_s(:long) %></span>
    <small><%= link_to 'Destroy', admin_comment_path(comment), method: :delete, remote: true, data: { confirm: 'Are you sure?' } %></small>
  </span>
  <p><%= comment.content %></p>
</li>